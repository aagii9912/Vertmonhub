# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local data imports (excel sources are not tracked)
*.xlsx

# typescript
*.tsbuildinfo
next-env.d.ts
debug.log
.env*.local

# Бодит хэрэглэгчийн өгөгдөл (HubSpot export, гэрээний жагсаалт) — хэзээ ч commit хийхгүй
*.csv
!data/**/*.csv
REPORTS/*.csv
REPORTS/*.xlsx

# DB backup dump-ууд (legacy хүснэгт устгахын өмнөх) — commit хийхгүй
supabase/backups/
scripts/eval/out/
