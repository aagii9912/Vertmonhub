'use client';

import { ContractsPage } from '@/components/contracts/ContractsPage';

/** /dashboard/contracts — v2 гэрээний жагсаалт. */
export default function ContractsRoute() {
    return <ContractsPage />;
}
