# Vertmon Hub — Кодын бүрэн review ба сайжруулах төлөвлөгөө (2026-09-11)

> `main` @ `e675753` (AI туслах v2 merge-ийн дараах төлөв). Read-only review — код өөрчлөөгүй.
> Арга зүй: автомат шалгалт (typecheck / lint / test / build / npm audit) + 6 чиглэлийн гүнзгий код review
> (API аюулгүй байдал, AI v2, CRM v2, DB/RLS, webhook/cron, frontend) + бодит DB-ийн схемийг read-only тулгасан.
> Доорх finding бүрийн «Батал.» багана: **код+DB** = би өөрөө эх код ба бодит DB-ээр шалгасан; **код** = эх кодоор шалгасан; **агент** = review агентын тайлан (файл:мөр иштэй, гэхдээ би дахин нээж үзээгүй).

---

## 0. Хураангуй

| Шалгалт | Үр дүн |
|---|---|
| `npm run typecheck` | ✅ алдаагүй |
| `npm run test` | ✅ 26 файл / 284 тест бүгд амжилттай |
| `npm run build` | ✅ амжилттай (1 deprecation: `middleware` → `proxy`) |
| `npm run lint` | ✅ 0 алдаа, ⚠️ 31 анхааруулга (ихэнх нь benign `set-state-in-effect`) |
| `npm audit --omit=dev` | ❌ 1 critical (`next` 16.1.1 → 16.3.4), 10 high (`xlsx` засваргүй, `ws`, `sharp` …) |
| Бодит DB (read-only) | ✅ кодын хүлээж буй бүх хүснэгт/багана байна; ❌ хэд хэдэн phantom объект, migration бүртгэл зөрүүтэй |

**Дүгнэлт.** Компиляц/тест талаас эрүүл, v2 UI (8 үндсэн хуудас) дизайн системээ сайн барьсан. Гэхдээ
**аюулгүй байдал ба multi-tenant тусгаарлалт**, **цагийн бүс**, **AI-ийн write tool-ын хяналт**, **webhook-ийн найдвартай байдал**,
**мониторинг (Sentry унтраастай)** гэсэн 5 чиглэлд production-д гарахаас өмнө засах ёстой асуудлууд байна.

| Түвшин | Тоо | Гол сэдэв |
|---|---|---|
| 🔴 Critical | 4 | auth-гүй route, хуурамч cookie-оор impersonation, DM бот гэрээний мэдээлэл задруулах, git-д PII |
| 🟠 High | 13 | AI write tool confirm-гүй, SSRF, cross-tenant read, mass-assignment, closed_won stub гэрээ, UTC «өнөөдөр», webhook ACK, Sentry, хоосон `properties`, storage policy, cron fail-open, RBAC write gating, IG давхар харилцагч |
| 🟡 Medium | ~25 | soft-delete шүүлт, cache, fail-soft, давхардал, phantom схем, migration hygiene, frontend бүтэц, dependency |
| 🟢 Low | олон | a11y, англи үлдэгдэл, давхардсан label map, dead code, root junk |

---

## 1. 🔴 Critical

| # | Асуудал | Файл | Батал. |
|---|---|---|---|
| C1 | **`POST /api/ai-assistant/analyze-messages` — ямар ч auth байхгүй**, `shopId`-г body-оос авч service-role-оор `chat_history` уншина. Одоогоор `select('role, content')` нь бодит баганад (`message, response, intent`) таарахгүй тул 500 буцаадаг — гэхдээ хэн нэгэн баганыг «засмагц» шууд дурын shop-ийн чатыг Gemini-ээр нэгтгэж задруулна. | `src/app/api/ai-assistant/analyze-messages/route.ts:8-24` | код+DB |
| C2 | **Хуурамч `vertmon-session` cookie-оор дурын хэрэглэгч болох.** 5 marketing route Supabase session байхгүй үед cookie-г энгийн base64 JSON гэж задалж `parsed.user_id`-д итгэдэг (гарын үсэг, шифр байхгүй). `vertmon-session=base64({"user_id":"<uuid>"})` тавиад `POST /api/marketing/facebook/publish` → тухайн хэрэглэгчийн Facebook page-д нийтлэл нийтэлнэ; insights/posts/instagram уншина. Хэрэглэгчийн UUID апп дотор (team, lead_activities.created_by) харагддаг. Энэ cookie-г одоо хэн ч олгодоггүй (`auth/login` зөвхөн цэвэрлэдэг) — зөвхөн халдагчид зориулсан зам. | `src/app/api/marketing/facebook/route.ts:24-33`, `facebook/insights`, `facebook/posts`, `facebook/publish`, `marketing/instagram/route.ts` | код |
| C3 | **DM бот `check_payment_status` — дурын Messenger хэрэглэгчид дурын харилцагчийн гэрээний санхүүг хэлнэ.** Зөвхөн `shop_id` + `ilike '%нэр/утас%'` шүүдэг, асууж буй хүний утастай холбодоггүй. «Батаагийн үлдэгдэл хэд вэ» → нэр, төлсөн, үлдэгдэл, хоцрогдол (1617 бодит гэрээ). `data.contracts`-д `customer_phone` ч буцна. | `src/lib/ai/services/ToolExecutor.ts:698-749`, `src/lib/ai/config/plans.ts:68` | код+DB |
| C4 | **Git түүхэнд бодит хүмүүсийн PII.** `all-contacts 2.csv` (10 594 мөр, 25 MB), `all-contacts.csv` (1 002 мөр) — HubSpot контакт export (нэр, утас, имэйл…), `REPORTS/lead-assignment.csv` (10 280 мөр, утасны дугаартай), `REPORTS/customers-not-in-hubspot.csv` (регистрийн дугаартай!), `Property Wrong Info…xlsx`. 2026-03-15 (`9612da0`)-аас хойш GitHub дээр. `.gitignore`-д `*.xlsx` байгаа ч аль хэдийн tracked. | repo root, `REPORTS/` | код+DB (файлын толгойгоор) |

## 2. 🟠 High

| # | Асуудал | Файл | Батал. |
|---|---|---|---|
| H1 | **AI orchestrator: 6 write tool `confirm`-гүй шууд ажилладаг, audit-д орохгүй.** `update_property_status / update_unit_status / update_property_price / update_lead_status / add_lead_note / process_contract_action` нь `confirm` параметргүй дуудагддаг → агент ажиллах явцад (`confirm=false`) шууд mutate хийнэ; `logAiAudit` зөвхөн `confirm=true` үед бичдэг тул эдгээр өөрчлөлт хэзээ ч бүртгэгдэхгүй. UI, CLAUDE.md, agents хуудас бүгд «баталгаажуулалттай» гэж мэдэгддэг. Prompt injection (lead-ийн нэрэнд FB-ээс ирсэн текст) → гэрээ cancel, unit available болгох боломжтой. `add_lead_note` нэр давхцвал `leads[0]`-д бичдэг. | `src/lib/ai/data-assistant/index.ts:82-87`, `functions.ts:515-703` | код |
| H2 | **SSRF: AI хавсралтын URL-ийг сервер шалгалтгүй татдаг.** `attachments[].url` (client-ээс ирсэн) → `fetch(att.url)` — host/scheme хязгаарлалтгүй, timeout-гүй, 8 MB хүртэл base64 болгож Gemini-д өгнө → дотоод сүлжээ/metadata URL унших, модель-ээр exfiltrate. | `src/lib/ai/orchestrator/runAgent.ts:75-88`, `http.ts:107-120` | код |
| H3 | **Cross-tenant харилцагчийн PII: `get_customer_insights(customer_id)`** салбар `.eq('shop_id')`-гүй → өөр shop-ийн харилцагчийн нэр/утас/имэйл/хаяг/notes. | `src/lib/ai/data-assistant/functions.ts:199-203` | код |
| H4 | **Төлбөрийн хуваарь PATCH — mass assignment.** `const { payment_id, ...updates } = body` → `.update(updates)`; Zod/allow-list байхгүй. `shop_id`, `contract_id`-г дарж бичиж мөрийг өөр tenant рүү шилжүүлж болно; `[id]` param шалгагддаггүй. | `src/app/api/dashboard/contracts/[id]/payments/route.ts:146-175` | код |
| H5 | **Lead-ийг «Амжилттай» (closed_won) болгоход хоосон гэрээ автоматаар үүснэ.** DB trigger `create_contract_on_lead_won` (SECURITY DEFINER) `total_price = conversion_value` (ихэвчлэн NULL), `sales_manager NULL`, `contract_date = today` stub үүсгэдэг. v2-ийн StatusPicker/bulk bar `closed_won`-ийг шууд зөвшөөрдөг (`/convert` route-ийг UI дууддаггүй) → 10 lead bulk → 10 үнэгүй гэрээ → contracts stats, director «units» өсөх, leaderboard-д орохгүй. | `supabase/migrations/20260608150000_link_contracts_to_leads.sql:19-35`, `src/app/api/dashboard/leads/[id]/route.ts:98-102`, `src/components/leads/pickers.tsx:62-72` | код |
| H6 | **«Өнөөдөр» серверт UTC-ээр тооцогддог.** `new Date().setHours(0,0,0,0)` Vercel дээр UTC шөнө дунд = УБ 08:00. Өглөө 07:30-д нээхэд «Өнөөдөр» tab өчигдрийн уулзалтуудыг харуулна, sidebar badge, «Өнөөдрийн ажил», my-stats, director сарын хил (1-ний 00:00–07:59 өмнөх сард), cron digest-ууд (`ai-digest` 09:00 УБ-д «өнөөдрийн лид» = 1 цагийн цонх) бүгд нөлөөлнө. Client талын `formatTime/formatShortDate` зөв (Asia/Ulaanbaatar), сервер тал буруу. | `src/app/api/dashboard/viewings/route.ts:28-29`, `nav-counts/route.ts:27-30`, `src/lib/utils/date.ts:79-105`, `src/lib/dashboard/my-stats.ts:65-75`, `director/route.ts:49-54`, `cron/ai-digest`, `cron/morning-leads` | код |
| H7 | **Webhook: Meta-д 200-г Gemini дууссаны дараа л буцаана; dedup-ийг боловсруулахаас ӨМНӨ бүртгэнэ; `maxDuration` алга.** Хамгийн муу тохиолдолд >60 с (Gemini 25 с × 2 + retry + Messenger retry). Meta timeout → дахин илгээнэ → `webhook_dedup`-д аль хэдийн байгаа тул алгасна → хариу ч, лид ч алга; давтагдвал Meta subscription-ийг throttle хийнэ. Мөн `webhook_jobs.payload`-д page access token plaintext хадгалагдана (одоо 0 мөр). | `src/app/api/webhook/route.ts:129-420` (dedup :199, 200 :420), `src/lib/webhook/retryService.ts:127-151` | код+DB |
| H8 | **Sentry огт ажиллахгүй.** `sentry.{client,server,edge}.config.ts` root-д байгаа ч Next 16 / Sentry v10-д `instrumentation.ts` + `instrumentation-client.ts` + `withSentryConfig` шаардлагатай — аль нь ч байхгүй (`next.config.ts` зөвхөн bundle-analyzer). Production-д алдааны мониторинг = 0. | `next.config.ts`, `sentry.*.config.ts`, `src/lib/monitoring/errorMonitoring.ts` | код |
| H9 | **DM AI (FB/IG бот) хоосон `properties` хүснэгтээс хайдаг.** Бодит DB: `properties` = 0 мөр, `property_units` = 2 544 мөр (Mandala импорт). `search_properties`, `show_property_images`, `schedule_viewing`, PromptService бүгд `properties`-ийг л уншдаг → бот ганц ч байр олохгүй. Мөн `WebhookService` DM бүрд `properties(*)` бүгдийг (зурагтайгаа) татдаг. | `src/lib/ai/services/ToolExecutor.ts:65,142,268`, `src/lib/webhook/WebhookService.ts:79-84` | код+DB |
| H10 | **Storage `products` bucket — нэвтэрсэн дурын хэрэглэгч дурын объектыг дарж/устгаж чадна.** Бодит DB-д owner-folder policy-ууд («Owner Update/Delete») ба permissive «Authenticated users can update/delete/upload» (`auth.role()='authenticated'`) зэрэгцэн байгаа тул OR болж permissive нь ялна. Bucket public. Upload route MIME/хэмжээ шалгадаггүй (HTML/SVG public URL). | бодит `storage.objects` policy; `supabase/migrations/20260119130000_create_storage.sql:15-30`; `src/app/api/dashboard/upload/route.ts:13-30` | код+DB |
| H11 | **Cron auth `CRON_SECRET` тохируулаагүй бол нээлттэй (fail-open); `overdue-check` `Bearer undefined` гэсэн шууд утгыг хүлээж авна; бүгд `===` (constant-time биш).** Vercel prod env-г шалгах эрх (MCP 403) байгаагүй тул prod-д тохируулагдсан эсэх нь баталгаагүй. Нээлттэй бол: `data-cleanup` (чат устгах), `overdue-check` (бүх хуваарийг overdue болгож push), digest-үүд (Gemini/Resend зардал). | `src/lib/auth/cron.ts:13`, `cron/overdue-check/route.ts:16`, `cron/data-cleanup/route.ts:20`, `cron/process-webhook-jobs/route.ts:44` | код (prod env — шалгаагүй) |
| H12 | **RBAC write gating ~12 mutating route-д алга; уншилтын хэд хэдэн route module шалгалтгүй.** `canWrite=false` (viewer) хэрэглэгч: `POST marketing/facebook/publish` (компанийн page-д нийтлэх), `leads/[id]/convert`, `customers/import/hubspot`, `integrations/hubspot/sync` (токен дарж бичих), `marketing/channels|contracts`, `dashboard/handover|service-logs|upload`, `sync-social`, `recompute-scores`. Уншилт: `dashboard/export/excel` (CRM бүхлээр export), `dashboard/conversations`, `customers/[id]`, `leads` GET/summary, `viewings` GET, `properties/search` — зөвхөн auth+shop. `landing-content` PUT — нэвтэрсэн дурын хэрэглэгч нүүр хуудсыг өөрчилнө. `user/switch-shop` `select('*')` → `hubspot_access_token` (plaintext) бүх member-т. | агентын хүснэгт (§API audit) | агент |
| H13 | **Instagram харилцагч давхардана, Messenger race `id:''` өгнө.** `UNIQUE(shop_id, facebook_id)` байна, `instagram_id`-д unique алга; select-then-insert → зэрэг 2 мессеж → 2 мөр → дараа нь `.single()` алдаа → мессеж бүрд шинэ мөр. Messenger-т unique барих ч ялагдсан insert `id:''` буцааж тухайн ээлж түүх/санах ойгүй явна, `create_lead`/`schedule_viewing` унана. | `src/lib/webhook/WebhookService.ts:209-263` | агент |

## 3. 🟡 Medium

### 3.1 Auth / session / RBAC
- **M1.** `src/middleware.ts:74-97` — «JWT signature шалгана» гэсэн коммент, бодит дээр base64 decode + `exp/sub` л шалгана; `SESSION_JWT_SECRET` байхгүй бол cookie байгаа л бол өнгөрнө. `jsonwebtoken` dependency огт ашиглагддаггүй. Нөлөө: `/dashboard`, `/admin` хуудасны shell (өгөгдөл API-аар хамгаалагдсан). Мөн Supabase cookie refresh алгасна. API талын `resolve-user.ts:9` / `admin/auth.ts:21` AES-GCM (service key-ээр) — аюулгүй, гэхдээ env байхгүй бол hardcoded fallback түлхүүр.
- **M2.** `admin/users` PATCH — `role` string validation-гүй (`super_admin` г.м дурын утга). Бодит DB-д `admins` хүснэгт **байхгүй** тул `getAdminUser()` = `user_roles.role='super_admin'` л гэсэн үг (агентын «support admin escalation» claim хүчингүй), гэхдээ `resolvePermissions`/`AuthContext`/`action route` бүр `admins`-ийг query хийж алдаа залгидаг (илүү round-trip).
- **M3.** `AuthContext.tsx:88-141` role/permissions-ийг browser-оос (`user_roles`, `admins`, `roles`, `role_permissions`) 4–5 дараалсан query-ээр авч, `user_roles`-д upsert оролддог (RLS `is_active_admin()` зогсооно — no-op) + 8 `[RBAC] console.log` prod-д. Sidebar/MobileNav/CommandPalette permissions байхгүй үед static map руу унадаг.
- **M4.** `shop_members` admin policy shop-оор хязгаарлагдаагүй (`user_roles.role='admin'` бол дурын shop-д өөрийгөө нэмнэ, `super_admin`-ийг оруулаагүй); `get_user_shop_id()` `UNION … LIMIT 1` ORDER BY-гүй → олон shop-той member-т санамсаргүй shop; `shops`, `shop_faqs`, `shop_quick_replies`, `shop_slogans`, `ai_question_stats` owner-only policy (member-үүд хоосон авна) — `features`, `shop`, `feedback` route-ууд ч owner-only.

### 3.2 Multi-tenant / өгөгдлийн бүрэн бүтэн байдал
- **M5. Soft-delete шүүлт дутуу:** `contracts` GET list+stats (`route.ts:36-40`), `contracts/[id]`, `leads/[id]` timeline (устгасан viewing/contract), `nav-counts` meetings badge, `properties/search`, `viewings/[id]` PATCH (устгасан viewing-д activity бичнэ); AI: `get_contracts_summary`, `get_contract_details`, `get_lead_details`; DM бот: `properties(*)` `deleted_at/status`-гүй, `show_property_images`, `schedule_viewing`; views `contract_statistics`, `customer_service_dashboard`, `contract_payment_status`, `meeting_monthly_summary`, `property_units_with_buyer` LATERAL.
- **M6. Phantom схем (код бодит DB-д байхгүй объект уншдаг):** `admins` (дээр), `plans/subscriptions/invoices` + `shops.plan_id…` (`api/features` → бүх feature «free» default; `admin/dashboard`, `admin/invoices` эвдэрхий), `data_deletion_requests` (Meta data-deletion — доор), `ai_memory` хүснэгт (`cleanup_expired_ai_memory()` шөнө бүр алдаа), `chat_history.role/content` (`dashboard/stats` recent chats үргэлж хоосон), `ai_conversations.started_at` (`ai-settings` stat = 0), `exec_sql` RPC (`admin/setup`).
- **M7. Meta data-deletion callback юу ч устгадаггүй** — `facebook_user_id/instagram_user_id` гэсэн байхгүй баганаар шүүж алдаа авсан ч Meta-д «completed» буцаана (compliance эрсдэл); signature compare constant-time биш. `src/app/api/meta/data-deletion/route.ts:48,98-101`.
- **M8. Гэрээний upsert `onConflict: 'shop_id,contract_number'` partial unique index-тэй (`WHERE deleted_at IS NULL AND contract_number IS NOT NULL`) таарахгүй** → PostgREST `ON CONFLICT` алдаа → plain insert fallback → давхардал unique-д унана. Үр дүн: «re-import шинэчилнэ» гэсэн зан төлөв ажиллахгүй, алдаа өгнө. `src/app/api/dashboard/contracts/route.ts:318-345`.
- **M9. Migration бүртгэл зөрүүтэй:** `supabase_migrations.schema_migrations` `20260624130000`-д зогссон, дараагийн 15 migration (leads soft delete, orchestrator, user_tasks, marketing budget, lead_activities …)-ийн объектууд DB-д байгаа ч бүртгэлгүй (гараар тавьсан). `20260426` давхар version, `20260322000000`-д `DROP TABLE … CASCADE`, `20260701120000` үүсгээд `20260701140000` устгадаг `sales_targets`, IF NOT EXISTS-гүй CREATE-ууд, `get_user_shop_id` 4 удаа, `mandala_setup.sql` policy-г WITH CHECK-гүй дахин үүсгэдэг.
- **M10. `rate_limits` хүснэгт 40 472 мөр, хамгийн хуучин 2026-05-01** → `cleanup_rate_limits` pg_cron ажиллахгүй байна; middleware API хүсэлт бүрд Edge-ээс Supabase RPC дууддаг (латенси + service key Edge-д), fail-open; түлхүүр `${ip}:${tier}` тул нэг NAT-тай оффис standard 100/мин, AI 20/мин-д хамтдаа багтана.

### 3.3 CRM v2 логик
- **M11. `Cache-Control: max-age`** (`leads/summary` 15s, `nav-counts` 30s, `director` 30s) react-query invalidation-ийг хүчингүй болгож (статус солиход tab count/badge 15–30 с хуучин), shop сольсны дараа эхний хариу өмнөх shop-ийнх байж болно.
- **M12. Fail-soft алдаа нуух:** `activities` POST `logLeadActivity` алдааг залгиад `201 {activity:null}` + UI «хадгалагдлаа»; `leads/[id]` sub-query алдаа → хоосон timeline 200; `summary` алдаа → 0; frontend-д 22 файл `console.error` л хийж «өгөгдөл алга» харуулна.
- **M13. Давхардал:** QuickCreateSheet ⌘↵ давхар submit (`saving` шалгадаггүй); offline outbox replay idempotency-гүй (timeout-ын дараа 2 дахь лид), flush үеийн `x-shop-id`/хэрэглэгчээр явна, хязгааргүй retry; viewings find-or-create phone/name `.limit(1)` order-гүй → буруу lead-д залгаж статус солино, зэрэг хүсэлт 2 лид; `(shop_id, customer_phone)` unique алга; phone «format-agnostic» match 4 оронтойгоор хувааж бичсэн формат таарахгүй.
- **M14. Статусын машин:** cancel/no-show уулзалт lead-ийг `viewing_scheduled`-д үлдээнэ (`viewing_scheduled_at` хэвээр → «Уулзалт 14:00» 24 ц); walk-in `new` lead-ийг `new` үлдээнэ; `lost_reason` server талд заавал биш.
- **M15. Төлбөр:** хуваарийн мөрийг «Төлсөн» болгоход `property_contracts.paid_amount/balance/overdue_days` шинэчлэгдэхгүй (trigger ч, route ч) → ContractDetail-д хоёр зөрүүтэй үлдэгдэл; list stats, my-stats overdue хуучин.
- **M16. Contracts API:** `search` `.or()`-д шууд (таслал → 400 → 500), `sortBy` whitelist-гүй, хуудас бүрд бүх (1000-мөр loop) гэрээг татаж memory-д slice; `contracts/[id]` DELETE hard delete (AI soft) ; `['managers', shopId]` query key-г 2 өөр fetcher хуваалцдаг (inactive менежер dropdown-д).
- **M17.** `LeadsPage` `checked` filter/page солиход цэвэрлэгдэхгүй; `LeadPanel`/`ContractDetail` 404-д мөнхийн skeleton; `?status=x`/`preferred_type` дурын string → enum 500.

### 3.4 AI туслах v2
- **M18.** `conversationId` бичихдээ эзэмшигч/shop шалгадаггүй (`http.ts:157-176`, `action/route.ts:86-95`) → өөр хүний харилцан яриа руу мессеж нэмнэ.
- **M19.** `maxDuration=60` vs client 90 с; global deadline алга; 3-агент төлөвлөгөө + `withRetry` 4 оролдлого × 26 Gemini дуудлага → 60 с-д Vercel таслахад `done/error` явахгүй, `persistAssistantExchange` ажиллахгүй (яриа алдагдана); `req.signal`/`cancel()` уншдаггүй → «Зогсоох» дарсан ч сервер 60 с ажилласаар.
- **M20.** «Үргэлж зөвшөөрөх» `create_contract`, `update_property_price`, `create_property`-д ч зөвшөөрөгддөг, per-shop боловч per-user биш; `invite_user`-ийн түр нууц үг `ai_messages`-д хадгалагдана; synthesis retry `token_reset`-гүй (давхар текст); retry-д алдсан user turn 2 удаа явна; `.or()`-д `customer_search` шууд.
- **M21.** DM бот (`AIRouter.ts:188-283`) `retryOperation` tool execution-ийг ч дахин ажиллуулдаг → 429 үед `schedule_viewing`/`create_lead` давхардана; comment event dedup-гүй; `is_echo` шүүлтгүй; `request_human_support` `ai_paused_until` тавьдаггүй (менежер хариулах хүртэл бот үргэлжлүүлнэ); FB profile name prompt-д sanitize-гүй; overdue-check 1000-мөр cap (1617 гэрээ).

### 3.5 Frontend бүтэц / performance
- **M22.** `x-shop-id` толгойг **32 файлд 61 газар гараар** бичсэн (`dashboardFetch` ашиглагч 8 л байна), 21 файлд `localStorage.getItem('vertmonhub_active_shop_id')` 44 удаа, 7 хуудас `SHOP_KEY`-г дахин зарласан; **27 client файл ямар ч shop толгойгүй** `/api/*` дууддаг (сервер «эхний shop» руу унадаг — settings, ai-settings бүх tab, surveys, marketing/social, useConversations, FeedbackWidget).
- **M23.** 12 хуудас + AuthContext **browser-оос Supabase-д шууд** (`marketing/*` 5 хуудас insert ч хийдэг, `marketing-roi`, `reports/leads`, `properties/[id]`) → зөвхөн RLS-д найдсан; `useRealtimeNotifications` гадаад mixkit mp3 тоглуулна.
- **M24.** `next/dynamic` огт алга; `AiChat` (AppShell-д үргэлж mounted) `recharts`-ийг static import → **dashboard бүх хуудас recharts ачаална**; `motion` 3 хуудас; 2 өөр DnD сан; 58/65 хуудас `'use client'`, `dashboard/layout.tsx` client; auth waterfall 5–6 дараалсан хүсэлт; `pipeline` `pageSize=1000`; `marketing/social` 1 293 мөр нэг файл; 37 файл effect-д fetch abort-гүй.
- **M25.** Хоосон/алдааны төлөв: 22 файл `console.error` л; `DirectorDashboard`, `ContractsPage`, `agents`, `ManagerSelector`, `useConversations`, `useFeatures`, `useMyStats` error branch-гүй; `confirm()` ×7, `alert()` ×2 (`confirmToast` байгаа ч 1 газар л ашигласан); client `console.*` 76.

### 3.6 Dependency / ops
- **M26.** `next` 16.1.1 — critical advisory (fix 16.3.4); `xlsx` prototype pollution/ReDoS (засваргүй — сервер талд admin import-д л ашигладаг, `exceljs`-рүү шилжих), `ws`, `sharp`(next-ээр), `@xmldom/xmldom`, `nanoid` г.м; `@supabase/auth-helpers-nextjs` (deprecated) + `@supabase/ssr` хоёулаа; `jsonwebtoken`, `pg`, `@types/*` prod dependencies-д; `puppeteer` + `playwright` хоёулаа devDeps.
- **M27.** CI: `lint` `continue-on-error: true` (хэзээ ч унахгүй); e2e ажиллуулдаггүй, e2e spec-үүд хуучирсан (`workspace-switcher.spec.ts`, `admin-plan-change.spec.ts`, `ui_playground.spec.ts` — устгасан UI-г заана); build үед `CRITICAL: FACEBOOK_VERIFY_TOKEN environment variable is required` log (import-time env шалгалт), recharts SSR warning.
- **M28.** CSP `script-src 'unsafe-inline' 'unsafe-eval'` (nonce-гүй); `next.config` `images.remotePatterns` ок; `middleware` → `proxy` convention (Next 16 deprecation).

## 4. 🟢 Low (товч)
- Lint 31 анхааруулга бүгд benign (mount-time hydration setState; `properties/[id]/page.tsx:50` hoisted function — cosmetic).
- a11y: `LeadsPage`/`ContractsPage` мөр `<tr onClick>` keyboard-гүй; `admin/users` ×4, `admin/roles`, `surveys/[id]`, `VirtualTour` гараар бичсэн modal (Escape/focus trap/`role="dialog"`-гүй); ~150 input label-гүй (`tasks`, `ContractDetail`, `QuickCreateSheet`); 8 `<img alt>`-гүй.
- Англи үлдэгдэл: `ui/Dialog` «Close», «Pipeline», `marketing/social` «Page Insights…», `admin/roles` «Module/canWrite», `admin/settings` «Max Tokens», HubSpot modal-ууд; `customer-service:555` «үйлчилгээний хүсэлт».
- Давхардсан логик: lead source label map ×6 (`marketing-roi` зөрсөн: `referral: 'Танилын'`), мөнгөний форматлагч ×9 (`admin/dashboard` `₮` урд), relative-date ×4, header helper ×15.
- Дизайн систем: v2-ийн 8 үндсэн хуудас 0 зөрчилтэй ✅; `ImportTab` gradient ×2, `FeedbackWidget`, `MortgageCalculator`, `global-error.tsx` Terracotta `#C2613F`, `--radius-3xl` үлдсэн, `rounded-2xl` ×31, `shadow-2xl` (`Sheet`, `Command`).
- Dead code (0 importer): `components/FeatureGate, NotificationButton, ThemeToggle, charts/{AgingBar,LineChart,Sparkline}, chat/{ChatInput,MessageBubble,ProductCardInChat,TypingIndicator,ToolProcessingIndicator}, dashboard/ConversationItem, empty-states/EmptyCart`; `hooks/useDashboard, usePWAInstall`; `lib/ai/{index,analytics,experiments/abTesting,i18n/messages,resilience/circuitBreaker,services/ProductParser,tools/definitions/{core,customer},validation/schemas}`, `lib/{errors,monitoring,services,webhook}/index.ts`, `lib/webhooks.ts`, `lib/supabase-middleware.ts` (CLAUDE.md-д «middleware ашигладаг» гэсэн нь хуучирсан), `lib/utils/{ai-preview,api-response,mobile-utils}`, `lib/validations/index.ts`.
- Legacy e-commerce үлдэгдэл: webhook `ORDER_` postback («захиалах… 🛒»), data-assistant `list_orders`/`get_product_stats`, DB-д `orders/order_items/products/discount_schedules/pending_messages` хүснэгт, `webhook_configs.event` CHECK `order.completed`; CLAUDE.md-д «customers.total_orders… үлдсэн» гэсэн нь буруу (20260608160000-д устгасан).
- Repo root: `check-db.ts`, `add_envs.sh`, `update_landing.js`, `e2e-screenshots/`, `test-results/`, `.DS_Store`, `AGENTS.md` (CLAUDE.md-тэй давхардсан, хуучирсан), `docs/` 30+ roadmap.

## 5. Агентын claim-уудаас шалгаад няцаасан / буурсан
| Claim | Бодит байдал |
|---|---|
| «`ads-insights-sync`, `customer-followups`, `social-insights-sync` зөвхөн POST → cron хэзээ ч ажилладаггүй» | ❌ 12 cron route бүгд GET+POST export хийдэг |
| «`support`/`admin` түвшний admins мөр өөрийгөө super_admin болгоно» | `admins` хүснэгт prod DB-д **байхгүй** → зөвхөн `user_roles.super_admin` өнгөрнө; `role` validation-гүй нь хэвээр (M2) |
| «`property_contracts (shop_id, contract_number)` UNIQUE алга» | `uq_contracts_shop_number` partial unique index prod-д **байна**; асуудал нь upsert-тэй таарахгүй (M8) |
| «`lead_funnel` view SECURITY DEFINER» | prod-д `security_invoker=true` |
| «Browser `user_roles` upsert → privilege escalation» | RLS write policy `is_active_admin()` → энгийн хэрэглэгчид no-op |
| «`webhook_jobs.payload`-д токен plaintext» | Загвар зөв ч одоо хүснэгт хоосон (0 мөр) — latent |

---

## 6. Сайжруулах төлөвлөгөө

Зарчим: (1) эхлээд tenant/auth цоорхойг хаах, (2) дараа нь найдвартай байдал ба өгөгдлийн зөв байдал, (3) дараа нь кодын эрүүл мэнд, (4) дараа нь дахин гарахгүй болгох сүлжээ (тест/lint дүрэм). Хэмжээ: **S** ≤ ½ өдөр, **M** 1–2 өдөр, **L** 3+ өдөр.

### Wave 0 — Даруй хаах (1–2 өдөр, hotfix branch, main-д шууд)
| # | Ажил | Файл | Хэмж. |
|---|---|---|---|
| 0.1 | `analyze-messages` route-ийг устгах (эсвэл `resolveApiUser` + shop membership + `requireModule('ai-assistant')` + баганыг `message/response` болгох) | `api/ai-assistant/analyze-messages` | S |
| 0.2 | 5 marketing route-оос base64 cookie салбарыг устгаж `getUserId()`/`getUserShop()` ашиглах; `middleware.ts`-ийн `vertmon-session` салбар + `SESSION_JWT_SECRET`-ийг устгах; `resolve-user.ts`/`admin/auth.ts`-ийн hardcoded fallback түлхүүр устгах (env байхгүй бол throw) | `api/marketing/facebook/**`, `marketing/instagram`, `middleware.ts`, `lib/auth/resolve-user.ts`, `lib/admin/auth.ts` | S |
| 0.3 | `check_payment_status`-ийг асуугчийн `customers.phone`/`phone_normalized`-той заавал холбох (утас баталгаажаагүй бол «Утасны дугаараа өгнө үү» → `collect_contact_info`), `customer_phone`-ыг хариунаас хасах; түр зуур `plans.ts`-д унтраах | `lib/ai/services/ToolExecutor.ts:698`, `lib/ai/config/plans.ts` | S |
| 0.4 | 6 write tool-д `confirm` gate + preview нэмэх (`createLead`-ийн загвараар), `add_lead_note` нэр давхцвал preview-д сонголт; `logAiAudit`-ийг эдгээрт ч бичих; `WRITE_TOOL_NAMES` тест | `lib/ai/data-assistant/index.ts:82-87`, `functions.ts:515-703`, `tools.ts` | M |
| 0.5 | Хавсралт fetch: зөвхөн `NEXT_PUBLIC_SUPABASE_URL/storage/v1/object/public/products/<shopId>/…` prefix, `AbortSignal.timeout(10s)`, redirect хориглох; upload route-д MIME allow-list (image/*, pdf), ≤ 4 MB, өргөтгөлийг MIME-ээс | `lib/ai/orchestrator/runAgent.ts:75`, `api/dashboard/upload` | S |
| 0.6 | `get_customer_insights` customer_id салбарт `.eq('shop_id', shopId)`; `get_contracts_summary/details`, `get_lead_details`-д `runExcludingDeleted` | `lib/ai/data-assistant/functions.ts:199,274,310` | S |
| 0.7 | Payments PATCH: Zod схем (`amount, paid_amount, due_date, paid_date, status, note` л), `contract_id = params.id` шалгах; мөн `service-logs` POST, `leads/[id]` `preferred_type` enum | `api/dashboard/contracts/[id]/payments`, `service-logs`, `leads/[id]` | S |
| 0.8 | Cron: `isAuthorizedCron` `CRON_SECRET` байхгүй бол `false` (dev-д `.env.local`-д тавих), `timingSafeEqual`; `overdue-check`, `data-cleanup`, `process-webhook-jobs`-ийн өөрийн хувилбарыг нэг helper болгох; Vercel prod env-д `CRON_SECRET`, `FACEBOOK_VERIFY_TOKEN`, `TOKEN_ENCRYPTION_KEY`, `TURNSTILE_SECRET_KEY` байгааг гараар баталгаажуулах | `lib/auth/cron.ts`, `api/cron/**` | S |
| 0.9 | Storage: migration — `products` bucket-ийн «Authenticated users can update/delete/upload» 3 policy-г DROP, owner-folder-ийг `shop_id`-folder (`get_user_shop_ids()`) болгох (`property-images` загвар); upload route-ийн замыг `<shopId>/…` хэвээр | шинэ migration | S |
| 0.10 | RBAC: 12 mutating route-д `requireWrite()`/`requireModuleWrite(module)`; `export/excel`, `conversations`, `customers/[id]`, `leads*`, `viewings`, `properties/search`-д `requireModule`; `landing-content` PUT → super_admin; `user/switch-shop` `select('id,name,…')` (токенгүй); `hubspot_access_token`-ыг `tokens.ts`-ээр шифрлэх | агентын хүснэгт | M |
| 0.11 | Git-ээс PII устгах: `git filter-repo` (эсвэл BFG)-оор `all-contacts*.csv`, `hubspot-listing-*.csv`, `REPORTS/*.csv`, `*.xlsx`-ийг түүхээс арилгаж force-push, GitHub-д cache арилгах хүсэлт, хамтрагчдад re-clone; `.gitignore`-д `*.csv`, `REPORTS/` | repo | S (зохицуулалт шаардана) |
| 0.12 | `next` → 16.3.4 (+ `eslint-config-next`), `npm audit fix` (ws, nanoid …); `xlsx` → `exceljs` (admin import + export) | `package.json` | S/M |

**Хүлээн авах шалгуур:** viewer-ээр `publish`/`convert`/`export` 403; хуурамч cookie 401; `analyze-messages` 404/401; AI-д «X гэрээг cancel хий» → карт гарч ирнэ, audit мөр; SSRF URL-д «татаж чадсангүй»; `npm audit --omit=dev` critical 0.

### Wave 1 — Найдвартай байдал ба өгөгдлийн зөв байдал (1–2 долоо хоног)
| # | Ажил | Хэмж. |
|---|---|---|
| 1.1 | **Sentry асаах:** `src/instrumentation.ts` (server/edge import), `src/instrumentation-client.ts`, `withSentryConfig(nextConfig)`, `global-error.tsx` → `Sentry.captureException`; root `sentry.*.config.ts` устгах; `safeErrorResponse`-оос Sentry рүү; alert босго | S |
| 1.2 | **Цагийн бүс:** `lib/utils/date.ts`-д `ubDayRange(date?)`, `ubMonthRange()`, `ubWeekRange()` (Asia/Ulaanbaatar, `Intl`/`Temporal`-гүйгээр offset +08:00 — Монгол DST-гүй); `viewings`, `nav-counts`, `my-stats`, `director`, `kpi-report`, бүх cron (`ai-digest`, `morning-leads`, `weekly-report`, `channel-expiry`, `task-reminders` «24h»)-д хэрэглэх; `getStartOfToday/getStartOfPeriod`-ийг устгах; unit test (07:30 УБ = 23:30 UTC өмнөх өдөр) | M |
| 1.3 | **Webhook:** signature + dedup → `webhook_jobs`-д enqueue → 200 (< 1 с); боловсруулалтыг `after()` (Next 16) эсвэл `process-webhook-jobs` cron (одоо 5 мин — 1 мин болгох эсвэл Supabase queue) руу; dedup-ийг «processing» төлөвтэй, амжилтгүй бол retry-д нээх; `export const maxDuration = 60`; `is_echo` шүүх; comment dedup; `retryOperation`-оос tool execution-ийг гаргах; `payload`-д токен биш `shop_id` хадгалж ажиллах үед decrypt | L |
| 1.4 | **DM AI ↔ `property_units`:** `search_properties`/`show_property_images`/`schedule_viewing`/PromptService-ийг `property_units` (+`property_units_with_buyer`) дээр ажиллуулах эсвэл units → `properties` sync view; `is_active/status/deleted_at` шүүлт; DM бүрд бүх inventory татахаа больж tool-оор л хайх | M |
| 1.5 | **closed_won:** StatusPicker/bulk-д `closed_won`-ийг «Гэрээ үүсгэх» урсгал руу чиглүүлэх (`/convert` эсвэл `contracts/generate?lead=`), API-д `closed_won`-ийг `total_price`/`contract_id`-гүйгээр хориглох; trigger-ийг `total_price IS NOT NULL` үед л stub үүсгэдэг болгох (эсвэл устгах); одоо үүссэн хоосон stub-уудыг цэвэрлэх SQL | M |
| 1.6 | **Soft-delete helper:** `activeOnly(q)` нэг helper, бүх read (§M5)-д; views-ийг `deleted_at IS NULL`-тэй дахин үүсгэх migration | M |
| 1.7 | **Cache/fail-soft:** `Cache-Control: max-age`-ийг `private, no-store` болгох (react-query өөрөө cache-лэнэ); `activities` POST алдаа бол 500; `leads/[id]` sub-query алдааг `missing[]`-ээр буцаах; frontend-д 22 файлд error state + toast | M |
| 1.8 | **Давхардал/race:** QuickCreateSheet `saving` guard; outbox-д `client_request_id` (UUID) + сервер талд idempotency (leads.client_request_id unique, 24 ц); outbox мөрд `shop_id`+`user_id` хадгалж зөрвөл хаях, retry cap; viewings find-or-create: `phone_normalized` баганаар тэнцүү match, `(shop_id, customer_phone_normalized) WHERE deleted_at IS NULL` unique + upsert; IG: `UNIQUE(shop_id, instagram_id)` + `getOrCreateCustomer` upsert `ON CONFLICT … RETURNING` | M |
| 1.9 | **Статус/төлбөр:** viewings PATCH cancelled/no_show → lead `viewing_scheduled_at=null`, статус `contacted` (closed биш бол); walk-in `new`→`contacted`; payments PATCH → гэрээний `paid_amount/balance/overdue_days` дахин тооцох (trigger `recompute_contract_totals`); `lost_reason` server-д заавал | M |
| 1.10 | **Contracts API:** `search` sanitize (leads-ийн загвар), `sortBy` whitelist, server pagination (`range`) + stats-ийг SQL aggregate/RPC-ээр; DELETE soft | S |
| 1.11 | **Phantom схем цэвэрлэх:** `admins` fallback-ийг устгаж `user_roles` л ашиглах; `api/features`/`plans.ts`-ийг DB-гүй static config болгох, `admin/dashboard|invoices`-ийн plans хэсгийг устгах; data-deletion route баганыг `facebook_id/instagram_id` + `data_deletion_requests` migration; `cleanup_expired_ai_memory`-г устгах/`customers.ai_memory` TTL болгох; `dashboard/stats` recent chats баганыг засах; `ai-settings` `started_at`→`created_at` | M |
| 1.12 | **DB hygiene migration:** applied version-уудыг `schema_migrations`-д бүртгэх (нэг удаагийн INSERT), `rate_limits` cleanup-ийг `data-cleanup` cron-д нэмэх (pg_cron унтарсан бол), `shop_members` policy shop-scope + super_admin, `get_user_shop_id()`-г `get_user_shop_ids()`-ээр солих (owner-only policy-уудыг member болгох), индексүүд: `leads(shop_id, sales_manager_name) WHERE deleted_at IS NULL`, `leads(shop_id, created_at DESC)`, `leads(shop_id, next_followup_at)`, `leads.customer_phone_normalized`, `property_viewings(shop_id, scheduled_at) WHERE deleted_at IS NULL`, `chat_history(customer_id, created_at DESC)`, `push_subscriptions(user_id)`, `channel_contracts(end_date) WHERE status='active'` | M |
| 1.13 | **AI v2 reliability:** `conversationId` эзэмшил+shop шалгах; global deadline 50 с (planner→agents→synthesis budget), `req.signal` → `AbortController` → Gemini; хугацаа хэтэрвэл partial + `error` event + persist; synthesis `token_reset`; retry-д user turn давхардуулахгүй; `invite_user` message-ээс нууц үг хасах; «Үргэлж зөвшөөрөх»-өөс `create_contract/update_property_price/create_property` хасах, per-user key | M |
| 1.14 | **Public `/api/leads`:** Turnstile заавал (env байхгүй бол 503), origin-ийг header биш `shop.allowed_origins`-оор, `shop_id`-г landing-ийн `site_key`-ээр тодорхойлох (oldest shop биш), имэйлд `name` escape, Upstash rate limit | M |
| 1.15 | `request_human_support` → `ai_paused_until = now()+30m`; push payload `reason` 200 тэмдэгт; `overdue-check` pagination (`range` loop) | S |

### Wave 2 — Кодын эрүүл мэнд (2–3 долоо хоног, feature ажлын хажуугаар)
| # | Ажил | Хэмж. |
|---|---|---|
| 2.1 | `dashboardFetch`/`dashboardJson` бүх 61 газарт; ESLint `no-restricted-syntax`: `'x-shop-id'` string literal ба `localStorage.getItem('vertmonhub_active_shop_id')`-ийг `src/app`, `src/components`, `src/hooks`-д хориглох (зөвхөн `lib/api/dashboardFetch.ts`-д зөвшөөрөх) | M |
| 2.2 | Browser Supabase query-уудыг API route болгох: `marketing/{ads,calendar,campaigns,messaging,sources,social,brand,analytics,page}`, `marketing-roi`, `reports/leads`, `properties/[id]`; `lib/supabase.ts` proxy-г client-д import хийхийг lint-ээр хориглох | L |
| 2.3 | `GET /api/me` (user + role + permissions + shops нэг хариу) → `AuthContext` 1 хүсэлт; browser upsert/console.log устгах; static role map fallback-ийг устгах | M |
| 2.4 | Bundle: `AiChat`-ийн chart-ийг `next/dynamic` (`ssr:false`), `ChartCard`, `motion` хуудсууд; `@hello-pangea/dnd` → `@dnd-kit` нэгтгэх; `pipeline` server pagination; `marketing/social`-ийг задлах | M |
| 2.5 | Dead code + legacy e-commerce устгах (§4 жагсаалт, webhook `ORDER_`, data-assistant `list_orders/get_product_stats`, `lib/supabase-middleware.ts`); DB-д legacy хүснэгтүүдийг backup-тай destructive migration (`orders, order_items, products, discount_schedules, pending_messages, ai_documents, ai_agents, satisfaction_surveys, hubspot_contacts?, facebook_tokens?`) | M |
| 2.6 | Dependency: `@supabase/auth-helpers-nextjs`, `jsonwebtoken`, `@types/*`→devDeps, `pg`→devDeps (scripts), `puppeteer` устгах; `middleware.ts` → `proxy.ts` (Next 16) | S |
| 2.7 | UI нэгтгэл: lead source/status/money/date map-уудыг `lib/leads/labels.ts`, `lib/utils/currency.ts`, `lib/utils/date.ts`-ээс л; `confirm()/alert()` → `confirmToast`; англи үлдэгдэл; a11y (мөр keyboard, modal → `ui/Dialog`, input label); `global-error.tsx` v2 өнгө; `--radius-3xl` устгах | M |
| 2.8 | CI: lint `continue-on-error` арилгах; e2e spec-үүдийг v2-д тааруулж (workspace-switcher, admin-plan-change, ui_playground устгах) preview deploy дээр ажиллуулах; `npm audit --omit=dev --audit-level=high` алхам | S |
| 2.9 | Баримт: CLAUDE.md-ийн хуучирсан заалтууд (customers legacy багана, supabase-middleware, AI «бүх write confirm-тэй»), `AGENTS.md` устгах/нэгтгэх, root junk файлууд, `docs/` roadmap-уудыг «хүчинтэй/хүчингүй» тэмдэглэх | S |

### Wave 3 — Дахин гарахгүй болгох сүлжээ (тасралтгүй)
- **API contract test:** route бүрийг `viewer` / `member` / `өөр shop-ийн хэрэглэгч` / `anon`-оор дуудаж 401/403/404-ийг батлах table-driven Vitest (msw биш, бодит handler `POST(new Request(...))`); шинэ route нэмэхэд заавал мөр нэмэх дүрэм.
- **RLS test:** Supabase local (`supabase start`) дээр policy бүрийг anon/authenticated JWT-ээр шалгах script (`scripts/rls-audit.ts` — «RLS on → policy байх ёстой», owner-only policy-г flag).
- **Env validation:** `src/lib/env.ts` (zod) — `CRON_SECRET`, `FACEBOOK_APP_SECRET`, `FACEBOOK_VERIFY_TOKEN`, `TOKEN_ENCRYPTION_KEY`, `SUPABASE_SERVICE_ROLE_KEY` prod-д заавал; import-time `CRITICAL` log-ийн оронд fail-fast.
- **Цагийн бүсийн тест** (1.2), **webhook flow тест** (Gemini mock, timeout, duplicate delivery), **AI tool gating тест** (бүх mutating tool `confirm=false` → preview л).
- **Хяналт:** Sentry alert (5xx rate, cron алдаа), `rate_limits`/`webhook_jobs` хэмжээний dashboard, Vercel cron log шалгалт долоо хоног бүр.
- **Review checklist** (`REVIEW.md`)-д нэмэх: shop scope, requireWrite, soft-delete, УБ цагийн бүс, `dashboardFetch`, confirm-gated AI tool.

---

## 7. Хавсралт

### 7.1 Бодит DB-ийн товч төлөв (2026-09-11, read-only)
- Хүснэгт: 80; бүгд RLS on; **policy-гүй (service-role only):** `admin_audit_log, ai_audit_log, bill_lines, chart_of_accounts, finance_audit_log, finance_transactions, lead_attribution_events, project_budgets, social_insights, vendor_bills, vendors`.
- View 9 — бүгд `security_invoker` ✅.
- Мөрийн тоо (ойролцоо): `property_contracts` 1 617, `property_units` 2 544, `customers` 1 006, `leads` 20, `properties` **0**, `rate_limits` 40 472, `ai_messages` 137, `sales_managers` 31, `shops` 1.
- `schema_migrations` сүүлийн бүртгэл `20260624130000`; local migration 127 файл; байхгүй объект: `admins, plans, subscriptions, invoices, data_deletion_requests, ai_memory, exec_sql, user_shops`.
- Storage: `products`, `property-images` хоёулаа public; `products`-д permissive update/delete policy (H10).

### 7.2 Ашигласан командууд
```bash
npm run typecheck && npm run lint && npm run test && npm run build
npm audit --omit=dev
# DB (read-only, node + pg, .env.local DATABASE_URL): pg_tables/pg_policies/pg_indexes/information_schema
```

---

## 8. Гүйцэтгэлийн явц (2026-09-11, салбар `fix/wave-0-security`)

### ✅ Хийгдсэн
- **Wave 0 бүхэлдээ** (0.1–0.12): C1–C4 (C4 = tracking-оос гаргасан, түүх цэвэрлэх нь хүлээгдэж байна), H1–H4, H10, H11, H12, M1, M2, M9 хэсэгчлэн; `next` 16.3.4, audit critical 0.
- **Wave 1 бүхэлдээ** (1.1–1.15): Sentry (H8), УБ цагийн бүс (H6), webhook `after()` ACK (H7), DM бот ↔ `property_units` (H9), closed_won guard + trigger (H5), soft-delete (M5), cache/fail-soft (M11, M12), давхардал (M13, H13), статус/төлбөр (M14, M15), contracts API (M16), phantom схем (M6, M7), migration бүртгэл + индекс + `rate_limits` цэвэрлэлт (M9, M10), AI v2 reliability (M18–M20), public leads (M11), `request_human_support` pause (M21 хэсэгчлэн).
- **Wave 2 бараг бүхэлдээ**: 2.1 (`dashboardFetch` 45 файлд + lint дүрэм), 2.2 (browser Supabase → `/api/marketing/data/[table]` shop-scoped route + `/api/properties/[id]` + leads GET `from/to`; зөвхөн realtime subscription (`useRealtimeNotifications`) browser client-тэй үлдсэн), 2.3 (`/api/me`), 2.4 (AiChat recharts dynamic), 2.5 (35 үхмэл файл + e-commerce tool + `api/features`/`api/admin/invoices`), 2.6 (deps), 2.7 (label/мөнгө/огнооны helper нэгтгэл, `confirmToast`, admin dashboard CRM карт, монгол копи, a11y), 2.8 (CI lint/audit, e2e), 2.9 (CLAUDE.md, AGENTS.md, root junk).
- **Wave 3 хэсэгчлэн**: API route auth-guard статик тест (`src/app/api/__tests__/route-auth-guard.test.ts`), AI confirm-gate тест, УБ цагийн бүсийн тест, prod env шалгалт (`src/lib/env.ts` + instrumentation).
- Prod DB-д тавьсан migration: `20260911120000` (storage policy), `20260911130000` (client_request_id, IG unique, trigger, data_deletion_requests, shop_members policy, 7 индекс); `schema_migrations`-д 15 хуучин version бүртгэгдсэн.

### ⏳ Хүлээгдэж буй (эзэмшигчийн шийдвэр / дараагийн ээлж)
- **C4 git түүх:** `git filter-repo`-оор PII CSV-үүдийг түүхээс арилгаж force-push (хамтрагчид re-clone) — эзэмшигчийн зөвшөөрөл шаардлагатай.
- **H5 өгөгдөл:** өмнө үүссэн 10 хоосон stub гэрээ (`lead_id` тэй, `contract_number`/`total_price` NULL) — migration 20260911130000-ийн коммент дахь `UPDATE`-ийг гараар ажиллуулах эсэх.
- **H11 prod env:** `CRON_SECRET`, `TURNSTILE_SECRET_KEY`, `FACEBOOK_VERIFY_TOKEN`, `LEAD_SHOP_ID` Vercel prod-д байгааг баталгаажуулах (агентад эрх байгаагүй).
- Wave 2 үлдсэн: `xlsx` → `exceljs` (агент ажиллаж байгаа / merge хүлээгдэж байна), `middleware` → `proxy` convention (Sentry тохирол шалгах), хуучин e-commerce DB хүснэгтүүдийг backup-тай destructive migration-аар устгах.
- Wave 3 үлдсэн: RBAC/RLS-ийн бодит handler тест (viewer/member/өөр shop), webhook flow тест (Gemini mock), мониторингийн alert босго.
